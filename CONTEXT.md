# ANI-RSS 领域上下文

ANI-RSS 管理番剧订阅、资源获取、下载处理和媒体整理，并把处理结果通知到用户配置的外部服务。

## 订阅与资源

**番剧**：用户希望持续跟踪的一部作品或其中一个季度/剧场版。

**订阅**：用户为番剧选择资源来源、字幕组和处理规则后形成的持续追踪关系。

**资源**：来自 RSS 或站点的一个可下载内容，包含标题、集数、大小、发布时间和下载地址等信息。
_避免_：种子、文件。种子是资源的下载载体，文件是下载后的本地内容。

**字幕组**：发布或整理资源的来源组织，用于筛选资源和生成媒体文件名。

**候选资源**：针对同一订阅或集数，尚未被规则选定的资源集合。

**主资源**：当前订阅优先采用的资源。

**备用资源**：主资源不可用或不满足条件时，可以替代主资源的资源。

**合集**：包含多个集数或多个媒体文件的一次性资源。

## 下载与媒体处理

**下载器**：负责接收资源并执行下载、查询状态、删除、重命名或移动操作的外部服务。

**下载任务**：一个已经提交给下载器、尚未完成生命周期的下载操作。

**刮削**：根据番剧和集数从外部元数据服务获取标题、封面、评分、集数等媒体信息。

**重命名**：依据番剧、季、集数、字幕组和媒体规则，为下载后的媒体文件生成规范名称。

**洗版**：当同一集出现更高优先级或更符合规则的资源时，用新资源替换已有资源的处理过程。

**完结**：番剧已经不再有待追踪的后续内容，或用户明确将其标记为完成。

## 外部服务与事件

**资源源站**：提供 RSS、番剧列表或资源详情的外部站点，例如 Mikan、AniBT 和 AnimeGarden。Mikan 官方域名 `mikanani.me` 经代理偶发 522/超时；可用镜像 `mikanime.tv`（302 重定向回官方，但部分网络环境下更稳），可在订阅 RSS 地址或 `mikanHost` 中切换。

**元数据服务**：提供番剧和媒体资料的外部服务，例如 TMDB 和 Bangumi。

**通知事件**：会触发通知、文件移动、Emby 刷新等后续动作的业务状态变化，例如发现资源、开始下载和下载完成。

**下载完成**：下载器确认资源可处理，且本地或远端媒体文件已经满足后续整理条件的状态。

**媒体库**：用户用于保存和播放已整理媒体文件的本地或远端文件集合。
